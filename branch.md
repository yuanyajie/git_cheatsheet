# Branches

-   Creating a new branch

        git branch devel
-   Switching branches

        git checkout devel
-   Switching back to master

        git checkout master
-   Merging branches

        git merge devel
-   Delete branch devel

        git branch -d devel
-   Listing all the local branches
    
        git branch
